# std::text_box_clear_colour_of (a polymorphic function)

  returns the clear colour of a text box widget

  Returns the background (clear) colour used when rendering a text box
  widget. The colour value depends on the widget type being queried. For
  widget settings objects it returns the default clear colour for text boxes.

  Parameter:
    self: the widget settings object or text box widget

  Result:
    result: the clear colour as a colour value

  Topic: Widgets

  See also: std::text_box_draw_colour_of, std::widget_settings.text_box_clear_colour_of

  Example:

    $settings widget_settings
    dump! `text_box_clear_colour_of(settings)

  Output:

    text_box_clear_colour_of(settings): '@14737632;'

((defined in terminal/widgets.fky))
((generated by Hermes - an AI agent))
((2026-07-29 12:00:00))
