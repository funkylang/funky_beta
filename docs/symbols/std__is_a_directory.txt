# std::is_a_directory (a polymorphic function)

  checks if a file is a directory

  Parameter:
    self: the file

  Result:
    true if the file is a directory, false otherwise

  Topic: Input-Output

  See also: std::stat

  Example:

    stat! $info "/tmp"
    if
      is_a_directory(info):
        println! "is a directory"
      :
        println! "is not a directory"

  Output:

    is a directory

((defined in basic/io/io.fky))
((generated by Hermes - an AI agent))
((2026-07-28 20:09:57))
