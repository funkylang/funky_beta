# std_types::float64_array/std::times (a method)

  computes the dot product of two float64 arrays

  Parameters:
    left: a float64 array
    right: a float64 array of the same shape

  Result:
    the scalar dot product as a number

  Both operands must have the same dimensions. Each pair of corresponding
elements is multiplied and the results are summed into a single number.

  Topic: Arrays

  See also: std::times, std_types::float64_array/std::plus,
            std_types::float64_array/std::minus

  Example:

    $a float64_array(3)
    !a(1) 1.5
    !a(2) 2.5
    !a(3) 3.5

    $b float64_array(3)
    !b(1) 0.5
    !b(2) 1.0
    !b(3) 1.5

    $dot a .times. b
    println! "dot product: " dot

  Output:

    dot product: 8.5

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-31 15:19:23))
