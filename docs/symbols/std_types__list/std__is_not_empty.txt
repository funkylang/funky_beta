# std_types::list/std::is_not_empty (a method)

  returns whether the list is not empty or not

  Parameters:
    self: a list

  Result:
    not_empty: true if the list is not empty, false otherwise

  Topic: Lists

  See also: std_types::list/std::is_empty,
            std_types::list/std::length_of

  Example:

    $prices list(19 25 42)
    if is_not_empty(prices)
      :
        println! "prices available"
      :
        println! "no prices at all"
    if is_not_empty(empty_list)
      :
        println! "prices available"
      :
        println! "no prices at all"

  Output:

    prices available
    no prices at all

((defined in runtime_templates/list.template))
((generated by Hermes - an AI agent))
((2026-08-24 17:03:45))
