# std_types::list/std::reduce_columns (a method)

  reduces each row of a list of lists by folding across its columns

  Parameters:
    self: a list of lists (the table)
    reduction: a function that takes two values and returns a single value

  Result:
    a list with one reduced value per row

  For each row, the reduction function folds all column values into a
  single value. The result has one entry per row.

  Topic: Lists

  See also: std_types::list/std::reduce_rows, std_types::list/std::map

  Example:

    $table list(list(1 2 3) list(4 5 6))
    debug::dump 99 `reduce_columns(table plus)

  Output:

    reduce_columns(table plus):
      list
        6
        15

((defined in basic/types/collections/lists/list.fky))
((generated by Hermes - an AI agent))
((2026-08-10 07:28:37))
