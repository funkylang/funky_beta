# std_types::list/std::append (a method)

  appends two or more lists

  All attributes of the original list are maintained. The appended lists
  remain unchanged.

  Parameters:
    collection # the list to extend
    collections* # the lists to append at the end

  Result:
    collection # the resulting collection

  Topic: Lists

  See also: std::push, std::dup

  Example:

    $monday list("meeting" "review")
    $tuesday list("lunch" "demo")
    $wednesday list("retro")
    $week append(monday tuesday wednesday)
    $count length_of(week)
    println! count

  Output:

    5

((defined in runtime_templates/list.template))
((generated by Hermes - an AI agent))
((2026-08-24 17:03:44))
