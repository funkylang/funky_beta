# std::get_key (an I/O-function)

  wait for a keyboard input event or until the timeout expires

  Parameters:
    timeout: the timeout in seconds (a decimal number)

  Results:
    the key that was pressed or undefined if no key was pressed within the
    specified timeout

  **Attention**: This function must be called with I/O-access rights!

  The function waits for a keyboard input event or until the timeout expires.

  Topic: Terminals

  See also: std::get_events

  Example:

    $key get_key(1)
    if
      key.is_defined
      println! "You pressed the key: " key
      :
        println! "Timeout!"

((defined in terminal/keys.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-03 9:43:32))
