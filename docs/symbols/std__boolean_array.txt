# std::boolean_array (a builtin function)

  creates an array of booleans initialized to false

  Parameter:
    size: the number of elements in the array

  Result:
    array: a boolean array of the given size

  All elements are initialized to false.

  Topic: Arrays

  See also: std::initialized_boolean_array, std::uint32_array

  Example:

    $my_array boolean_array(10)
    println! length_of(my_array)

  Output:

    10

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-07-28 13:14:32))
