# std_types::key_order_set/std::first_item_of (a method)

  returns the first item in the key order set

  Parameter:
    self: the key order set

  Result:
    the first (smallest) item in the set, or undefined if the set
    is empty

  A key order set keeps its items sorted, so the first item is the
  smallest one.

  Topic: Sets

  See also: std_types::key_order_set/std::last_item_of,
            std_types::key_order_set/std::next_item_of,
            std_types::key_order_set/std::previous_item_of

  Example:

    $fruits key_order_set("cherry" "apple" "banana")
    println! first_item_of(fruits)

  Output:

    apple

((defined in basic/types/collections/sets/key_order_set.fky))
((generated by Hermes - an AI agent))
((2026-08-19 21:50:58))
