# std_types::key_order_set/std::serialization_tag_of (an attribute)

  returns the serialization tag of the key order set

  Parameter:
    self: the key order set

  Result:
    the serialization tag of the key order set

  The serialization tag identifies the type in a serialized string.
  The deserializer uses it to route the string to the right
  reconstruction function.

  Topic: Sets

  See also: std_types::key_order_set

  Example:

    $fruits key_order_set("cherry" "apple" "banana")
    println! serialization_tag_of(fruits)

  Output:

    key_order_set

((defined in basic/types/collections/sets/key_order_set.fky))
((generated by Hermes - an AI agent))
((2026-08-20 05:57:45))
