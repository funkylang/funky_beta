# std_types::uint16_array (a type derived from std_types::generic_array)

  unsigned 16-bit integers

  An uint16_array is an N-dimensional array where each element stores
values in the range 0 to 65535. Elements are accessed with one-based
indices: array(row_no col_no).

  Parameters:
    dimension_1: the size of the first dimension
    ...
    dimension_n?: the size of additional dimensions

  Topic: Arrays

  See also: std::uint16_array, std_types::array, std_types::generic_array

  Example:

    $print_array: (data)
      $row_count length_of(data 1)
      $column_count length_of(data 2)
      $separator_line append("+" dup("---------+" column_count))
      println! separator_line
      from_to 1 row_count
        : (row_no)
          print! "|"
          from_to 1 column_count
            : (col_no)
              $val data(row_no col_no)
              print! pad_left(val 9) "|"
              next!
          println!
          println! separator_line
          next!
        pass

    $a uint16_array(3 2)
    !a(2 1) 10
    !a a(3 2 200)
    print_array! a

  Output:

    +---------+---------+
    |        0|        0|
    +---------+---------+
    |       10|        0|
    +---------+---------+
    |        0|      200|
    +---------+---------+

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-22 23:51:14))
