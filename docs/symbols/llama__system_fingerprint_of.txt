# llama::system_fingerprint_of (a polymorphic function)

  the build fingerprint of the server that handled a completion

  Parameters:
    meta: a llama_types::metadata object from llama::complete

  Result:
    fingerprint: a system version hash, or undefined if not yet reported

  A fingerprint that identifies the specific build or configuration of the
  llama-server process. Useful for verifying which server instance handled a
  request, or detecting configuration drift across deployments. The format is
  defined by the server.

  Topic: AI

  See also: llama_types::metadata

  Example:

    <require basic/stdlib>
    <require ai/llama_server>

    <using std>
    <using llama>

    complete! $meta "/no_think/ Ping"
      METADATA
      MAX_TOKENS = 3
      TEMPERATURE = 0

    println! "Fingerprint: " debug::to_string(system_fingerprint_of(meta) 0 1)

  Output:

    Fingerprint: "b10840-73ab7599b"

((defined in ai/llama_server.fky))
((generated by Hermes - an AI agent))
((2026-09-17 12:45:50))
