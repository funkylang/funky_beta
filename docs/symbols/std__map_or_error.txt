# std::map_or_error (a polymorphic function)

  appliies a function to all items of a collection

  The function results are collected in a collection of the same type as the
  original collection.

  If any item ist mapped to an error object than the result ist not a
  collection but the first (left-to-right-order) error object created.

  The function applications might be executed in parallel!

  Topic: Collections

  See also: reduce, map_reduce, filter

  Example:

    $my_list create_list(1 2 3 4 5)
    $result std::map_or_error my_list (x -> x*x)
    println! result

    prints

    [1 4 9 16 25]

((defined in basic/types/collections/collection.fky))
((generated by gemma-2-27b-it.Q5_K.gguf))
((2024-07-03 21:30:17))
