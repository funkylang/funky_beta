# std_types::key_order_table/std::reduce (a method)

  folds a key order table into a single value by applying a binary function

  Parameters:
    self: the key order table to reduce
    reduce_function: the function to apply to pairs of values
    unit?: the value to return for an empty table

  Results:
    value: the reduced result

  The reduction order is not guaranteed, so the combine function must be
associative. A single-element table returns that element's value directly.
The unit value is only consumed when the table is empty.

  Topic: Tables

  See also: std_types::key_order_table/std::map,
            std_types::key_order_table/std::map_reduce

  Example:

    $cart
      key_order_table
        "apples" = 10
        "oranges" = 4
        "bananas" = 7

    $total reduce(cart plus)
    println! "total items: @(total)"

  Output:

    total items: 21

((defined in basic/types/collections/tables/key_order_table.fky))
((generated by Hermes - an AI agent))
((2026-08-23 22:47:13))
