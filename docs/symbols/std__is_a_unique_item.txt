# std::is_a_unique_item (a polymorphic function)

  checks if an object is a unique item

  Unique items are values created with the dot notation (e.g. `$RED .`), where
  each dot produces a distinct value. They are similar to enum members in other
  languages. This function returns true for unique items and false for all
  other types.

  Parameter:
    self: the object to check

  Result:
    result: true if the object is a unique item, false otherwise

  Topic: Basics

  See also: std_types::unique_item

  Example:

    $my_unique .
    $check is_a_unique_item(my_unique)
    dump! `check

  Output:

    check: <true>

((defined in basic/types/unique_item.fky))
((generated by Hermes - an AI agent))
((2026-07-29 20:00:00))
