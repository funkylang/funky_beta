# std::dialog_clear_colour_of (a polymorphic function)

  return the dialog background colour

  Returns the background (clear) colour used when rendering a dialog
  widget. The colour value depends on the widget type being queried. For
  widget settings objects it returns the default clear colour for dialogs.

  Parameter:
    self: the widget settings object or dialog widget

  Result:
    result: the clear colour as a colour value

  Topic: Widgets

  See also: std::widget_settings.dialog_clear_colour_of

  Example:

    $settings widget_settings
    dump! `dialog_clear_colour_of(settings)

  Output:

    dialog_clear_colour_of(settings): '@12632256;'

((defined in terminal/widgets.fky))
((generated by Hermes - an AI agent))
((2026-07-28 23:00:00))
