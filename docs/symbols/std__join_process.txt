# std::join_process (a polymorphic function)

  waits for a process to finish

  Parameters:
    self: the input-output object
    pid: the process ID of the process to wait for

  Result:
    self: the input-output object

  The function tells the event system (<std::get_events>) to send a
  <std::PROCESS_JOINED> event when the specified process has finished.

  Topic: Input-Output Events

  Example:

    create_process! $pid $in_fd $out_fd $err_fd command arguments
    ...
    join_process &io pid
    ...
    loop:
      get_events! &io $events
	for_each events
	  : (event)
	    event $type $epid $argument
	    case type
	      PROCESS_JOINED:
		if
		  pid == epid:
		    ...
((defined in basic/io/io_events.fky))
((generated by gemma-2-27b-it.Q5_K.gguf))
((revised))
((2024-07-03 21:06:56))
