# std_types::value_range/std::subtype_of (an attribute)

  returns "value_range"

  Parameter:
    self: an instance of <std_types::value_range>

  Result:
    subtype: the string "value_range"

  Identifies value ranges as a specific subtype of tuples. The subtype
  distinguishes value ranges from other tuple types, such as key-value pairs
  and sequences.

  Topic: Tuples

  See also: std_types::value_range, std::subtype_of

  Example:

    $my_range 'a'..'z'
    println! subtype_of(my_range)

  Output:

    value_range

((defined in basic/types/collections/tuples/value_range.fky))
((generated by Hermes - an AI agent))
((2026-08-30 18:19:08))
