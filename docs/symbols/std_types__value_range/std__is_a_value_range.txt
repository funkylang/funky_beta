# std_types::value_range/std::is_a_value_range (an attribute)

  checks if an object is a value range

  Parameter:
    self: the object to check

  Result:
    true if the object is a value range, false otherwise

  Topic: Tuples

  See also: std_types::value_range, std_types::object/std::is_a_value_range

  Example:

    $my_range 1..10
    if
      is_a_value_range(my_range)
      :
        println! "yes, it is a value range"
      :
        println! "no, it is not"

  Output:

    yes, it is a value range

((defined in basic/types/collections/tuples/value_range.fky))
((generated by Hermes - an AI agent))
((2026-08-30 15:59:03))
