# std_types::string/std::truncate_behind (a method)

  returns the substring ending with the specified expression

  Parameters:
    self: the string to truncate
    expr: the expression to look for
    nth: look for the nth occurrence of <expr> instead of the first one

  Result:
    the truncated string, or the original string when no match is found

  If the string contains the expression the substring ending with the first
  match of the expression is returned.

  If the string does not contain the expression then the string is returned
  without alteration.

  An optional third argument can be used to match the <nth> occurrence of
  the specified expression. If this value is negative then -1 means the
  last occurrence, -2 means the next-to-last occurrence and so on.

  Topic: Strings

  See also: std::until, std::truncate_before,
            std::truncate_from, std::truncate_until

  Example:

    $url "https://nousresearch.com/api/v1/docs"
    $scheme_part truncate_behind(url "/" 2)
    println! "scheme part: @(scheme_part)"
    $path "/home/hermes/projects/funky_beta/runtime/interpreter.fky"
    $dir_part truncate_behind(path "/" -1)
    println! "directory part: @(dir_part)"

  Output:

    scheme part: https://
    directory part: /home/hermes/projects/funky_beta/runtime/

((defined in basic/types/collections/strings/string.fky))
((generated by Hermes - an AI agent))
((2026-08-24 12:39:32))
