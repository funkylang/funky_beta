# std::uint32_array (a builtin function)

  creates an array of unsigned 32-bit integers initialized to zero

  Parameter:
    size: the number of elements in the array

  Result:
    array: a uint32 array of the given size

  All elements are initialized to zero.

  Topic: Arrays

  See also: std::initialized_uint32_array, std::boolean_array

  Example:

    $my_array uint32_array(5)
    println! length_of(my_array)

  Output:

    5

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-07-28 13:14:32))
