# std::on_websocket_message (a function)

  sets the websocket message handler for a websocket connection

  Parameters:
    io: the I/O-object
    fd: the file descriptor of the websocket connection
    handler: the websocket message handler

  Result:
    io: the I/O-object

  If the file descriptor is undefined then the default websocket message
  handler is set.

  The websocket message handler is called with the following parameters:

    io: the I/O-object
    fd: the file descriptor of the websocket connection
    message: the message received from the websocket connection

  Topic: Network Clients and Servers

  See also: std::on_key_press, std::on_mouse_click

  Example:

    $io create_io
    $server create_server(io 8080)
    $server.on_websocket_message:
      : (io fd message)
        log io 1 "got websocket message from @(fd.serialize): @(message)"
        -> io

((defined in web/terminal.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-03 16:20:40))
