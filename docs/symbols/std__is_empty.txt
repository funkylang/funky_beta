# std::is_empty (a polymorphic function)

  checks whether a collection is empty

  Parameter:
    collection: the collection to check

  Result:
    boolean: true if the collection is empty, false otherwise

  The following invariant always holds:

  is_empty(collection) == (length_of(collection) == 0)

  Topics: Collections, Strings, Lists

  See also: std::is_not_empty, std::length_of

  Example:

    $empty list()
    $filled list(1 2 3)
    $e is_empty(empty)
    $f is_empty(filled)
    debug::dump `e `f

  Output:

    e: true
    f: false

((defined in runtime_templates/aaa.template))
((generated by Hermes - an AI agent))
((2026-08-11 05:04:14))
