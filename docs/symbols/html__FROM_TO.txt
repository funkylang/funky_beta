# html::FROM_TO (a function)

  generates a sequence of elements from a range of indices

  Parameters:
    from: the starting index
    to: the ending index
    body: a function that generates the element for a given index

  Result:
    a string containing the generated elements

  The function is used to generate a sequence of elements from a range of indices.

  Topic: HTML

  Example:

    <require basic/stdlib>
    <require basic/export/html>

    <using std>
    <using html>

    println!
      FROM_TO 1 3
	: (idx)
	  P "This is paragraph @(idx)"

  Output:

    <p>This is paragraph 1</p>
    <p>This is paragraph 2</p>
    <p>This is paragraph 3</p>

((defined in basic/export/html.fky))
((generated by Qwen3-235B-A22B-Q4_K_M-00001-of-00003.gguf))
((rating 7))
((2025-05-10 18:56:13))
