# std_types::io/llama::insert_tokens (a method)

  inserts tokens into a sequence

  Parameters:
    io: the I/O-object to use
    model: the model to use
    sequence_id: the sequence to insert into
    position: the position to insert at
    tokens: the tokens to insert

  Result:
    none

  The specified tokens are inserted into the specified sequence at the
  specified position.

  Topic: AI

  See also: llama::add_tokens, llama::delete_tokens

((defined in ai/new_llama.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2025-04-20 10:04:26))
