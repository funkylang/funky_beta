# std::cancel (a polymorphic function)

  cancels a scheduled I/O job

  Parameters:
    io # the input-output object
    job_no # the job number to cancel

  Result:
    io # the updated input-output object

  Cancels a scheduled I/O job identified by its job number.

  Topic: Inout-Output Jobs

  See also: std::schedule, std::handle_jobs, std_types::io/cancel

  Example:

    $my_screen create_screen(terminal 40 25)
    $my_job std::schedule(my_screen 1000 print! "Hello, World!")
    std::cancel(my_screen my_job)

((defined in basic/io/jobs.fky))
((generated by gemma-2-27b-it.Q5_K.gguf))
((2024-07-03 21:18:59))
