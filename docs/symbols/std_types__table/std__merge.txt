# std_types::table/std::merge (a method)

  merges the two tables

  Parameters:
    self: the base table
    other: the table to merge
    merge_function: the function used to merge corresponding values

  Result:
    table: the merged table

  If a key is contained in both tables and *merge_function* is not specified
  then the value from the *other* table is used for the merged table.

  The *merge_function* is applied only if both tables contain a value for the
  same key.

  Topic: Tables

  See also: std_types::hash_table/std::merge,
            std_types::key_order_table/std::merge

((defined in basic/types/collections/tables/table.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-02 23:14:50))
