# std::find_first (a polymorphic function)

  returns the first item of a collection that fullfills the condition

  Parameters:
    self: the collection to search in
    test: a function that takes an item of the collection as argument and
          returns *true* if the item fullfills the condition

  Result:
    item: the first item that fullfills the condition

  If no item fullfills the condition then *undefined* is returned.

  Topic: Collections

  See also: std::filter

  Example:

    $my_list create_list(1 2 3 4 5)
    $result std::find_first my_list
      : (x)
        x > 3
    println! result

    prints

    4

((defined in basic/types/collections/collection.fky))
((generated by gemma-2-27b-it.Q5_K.gguf))
((2024-07-03 21:31:20))
