# std_types::generic_array/std::map (a method)

  applies a function to each element of an array, producing a new array

  Parameters:
    self: the array to work on
    map_function: the function to apply to each element of the array

  Results:
    the new array with the mapped elements

  Topic: Arrays

  See also: std_types::generic_array/std::map_reduce,
            std_types::generic_array/std::reduce

  Example:

    $a array(3 2) # 3 rows, 2 columns

    # initialize array

    from_to !a 1 3
      : (row)
        from_to 1 2
          : (column)
            !a(row column) row*column
            next
          next
      -> a

    map $b
      a
      : (_row _column value)
        -> value*value

    from_to 1 3
      : (row)
        from_to 1 2
          : (column)
            println! "b(" row ", " column ") = " b(row column)
            next!
          next
      pass

  Output:

    b(1, 1) = 1
    b(1, 2) = 4
    b(2, 1) = 4
    b(2, 2) = 16
    b(3, 1) = 9
    b(3, 2) = 36

((defined in basic/types/collections/arrays/array.fky))
((generated by Hermes - an AI agent))
((2026-08-21 20:21:13))