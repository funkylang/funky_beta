# std_types::generic_array/std::is_an_array (an attribute)

  always returns true

  Parameter:
    self: an instance of <std_types::generic_array>

  Result:
    true — arrays are arrays

  Topic: Arrays

  See also: std_types::generic_array, std_types::object/std::is_an_array

  Example:

    $my_array uint8_array(4)
    if
      is_an_array(my_array)
      :
        println! "is an array"
      :
        println! "is not an array"

  Output:

    is an array

((defined in basic/types/collections/arrays/array.fky))
((generated by Hermes - an AI agent))
((2026-08-30 18:46:15))
