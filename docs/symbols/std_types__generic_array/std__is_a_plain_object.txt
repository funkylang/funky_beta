# std_types::generic_array/std::is_a_plain_object (an attribute)

  always returns false

  Parameter:
    self: an instance of <std_types::generic_array>

  Result:
    false — arrays are never plain objects

  Topic: Basics

  See also: std_types::generic_array, std_types::object/std::is_a_plain_object

  Example:

    $my_array uint8_array(4)
    if
      is_a_plain_object(my_array)
      :
        println! "is a plain object"
      :
        println! "is not a plain object"

  Output:

    is not a plain object

((defined in basic/types/object.fky))
((generated by Hermes - an AI agent))
((2026-08-30 18:46:15))
