# std_types::hash_set/std::serialization_tag_of (an attribute)

  returns the serialization tag of the hash set

  Parameter:
    self: the hash set

  Result:
    the serialization tag of the hash set

  The serialization tag identifies the type in a serialized string.
  The deserializer uses it to route the string to the right
  reconstruction function.

  Topic: Sets

  See also: std_types::hash_set

  Example:

    $fruits hash_set("cherry" "apple" "banana")
    println! serialization_tag_of(fruits)

  Output:

    hash_set

((defined in basic/types/collections/sets/hash_set.fky))
((generated by Hermes - an AI agent))
((2026-08-20 05:57:45))
