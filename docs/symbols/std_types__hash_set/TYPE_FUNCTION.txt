# std_types::hash_set/: (a type function)

  retrieve, add, or remove a key

  Parameters:
    key: the lookup key
    value?: new state of the key (true to add, false to remove)

  Result:
    true or false on retrieval, or the modified set on add/remove

  Calling a <std_types::hash_set> behaves differently depending on
  the number of arguments.

  **One argument:** the key's state is retrieved. The call returns
  <std::true> if the key is present, <std::false> otherwise.

  **Two arguments:** the set is modified. The second argument must be
  a boolean: <std::true> adds the key, <std::false> removes it. A
  cloned set with the change is returned; the original set remains
  unchanged. If the key's state is not actually changed, the original
  set is returned unchanged.

  Topics: Sets, Collections

  See also: std::empty_hash_set, std_types::hash_table/:

  Topic: Arrays

  Example:

    $friends empty_hash_set
    !friends("Alice") true
    !friends("Bob") true
    !friends("Carol") true
    $present friends("Bob")
    dump! `present
    $grown friends("Dave" true)
    $reduced friends("Bob" false)
    dump! `grown `reduced

  Output:

    present: <true>
    grown: hash_set <4>
      "Dave"
      "Bob"
      "Alice"
      "Carol"
    reduced: hash_set <2>
      "Alice"
      "Carol"

((defined in basic/types/collections/sets/hash_set.fky))
((generated by Hermes - an AI agent))
((2026-08-19 14:29:47))
