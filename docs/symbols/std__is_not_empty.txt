# std::is_not_empty (a polymorphic function)

  checks whether a collection contains at least one item

  Parameter:
    collection: the collection to check

  Result:
    true if the collection is not empty, false otherwise

  The following invariant always holds:

  is_not_empty(collection) == !(is_empty(collection))

  Topics: Collections, Strings, Lists

  See also: std::is_empty, std::length_of

  Example:

    $empty list()
    $filled list(1 2 3)
    $e is_not_empty(empty)
    $f is_not_empty(filled)
    debug::dump `e `f

  Output:

    e: false
    f: true

((defined in runtime_templates/aaa.template))
((generated by Hermes - an AI agent))
((2026-08-11 05:28:47))
