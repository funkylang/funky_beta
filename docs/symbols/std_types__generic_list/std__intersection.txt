# std_types::generic_list/std::intersection (a method)

  computes the intersection of two collections

  Parameters:
    left: the first collection
    right: the second collection

  Result:
    collection: the intersection of the two collections

  The result shares type with the first collection.

  Topic: Lists

  See also: std_types::generic_list/std::merge

  Example:

    $evens list(2 4 6 8)
    $factors list(4 3 8 9)
    $shared intersection(evens factors)
    debug::dump 99 `shared

  Error output:

    shared:
      list
        4
        8

((defined in basic/types/collections/lists/list.fky))
((generated by Hermes - an AI agent))
((2026-08-24 11:07:27))
