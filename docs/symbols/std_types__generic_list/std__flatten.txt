# std_types::generic_list/std::flatten (a method)

  flattens a list of lists ( ...of lists ...)

  Parameters:
    self: the list to flatten

  Result:
    list: the flattened list

  Topic: Lists

  See also: std_types::list/std::flatten

  Example:

    $nesting list(list(1 2) 3 list(4 5))
    $flat flatten(nesting)
    debug::dump 99 `flat

  Error output:

    flat:
      list
        1
        2
        3
        4
        5

((defined in basic/types/collections/lists/list.fky))
((generated by Hermes - an AI agent))
((2026-08-24 11:07:27))
