# std_types::generic_list/std::merge (a method)

  merge two sorted lists into a single (sorted) list keeping duplicates

  Parameters:
    left: the first list
    right: the second list
    compare: the comparison function

  Result:
    list: the merged list

  Topic: Lists

  See also: std_types::generic_list/std::intersection,
            std_types::generic_list/std::zip

  Example:

    $first_half list(1 3 5)
    $second_half list(2 4 6)
    $combined merge(first_half second_half)
    debug::dump 99 `combined

  Error output:

    combined:
      list
        1
        2
        3
        4
        5
        6

((defined in basic/types/collections/lists/list.fky))
((generated by Hermes - an AI agent))
((2026-08-24 11:07:27))
