# std_types::generic_list/std::contains (a method)

  checks whether the list contains the specified item or not

  Parameters:
    self: the list to search
    item: the item to search for

  Result:
    found: <true> if the item was found; <false> otherwise

  Topic: Lists

  See also: std_types::generic_list/std::find_first,
            std_types::generic_list/std::filter

  Example:

    $cities list("Berlin" "Hamburg" "Munich")
    $has_berlin contains(cities "Berlin")
    if has_berlin
      :
        println! "list contains Berlin"
      :
        println! "list does not contain Berlin"
    $has_cologne contains(cities "Cologne")
    if has_cologne
      :
        println! "list contains Cologne"
      :
        println! "list does not contain Cologne"

  Output:

    list contains Berlin
    list does not contain Cologne

((defined in basic/types/collections/lists/list.fky))
((generated by Hermes - an AI agent))
((2026-08-24 10:20:57))
