# std_types::generic_list/std::get (a method)

  removes and returns the first element of the list

  Parameters:
    self: the list to be modified

  Results:
    collection: the modified list
    item: the removed item

  Topic: Lists

  See also: std_types::generic_list/std::put, std_types::generic_list/std::pop,
            std_types::generic_list/std::push

  Example:

    $steps list("north" "east" "south")
    get $remaining_steps $first_step steps
    debug::dump 99 `first_step `remaining_steps

  Error output:

    first_step: "north"
    remaining_steps:
      list
        "east"
        "south"

((defined in basic/types/collections/lists/list.fky))
((generated by Hermes - an AI agent))
((2026-08-24 11:07:27))
