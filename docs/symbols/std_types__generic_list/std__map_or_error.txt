# std_types::generic_list/std::map_or_error (a method)

  applies a function to all elements of the list

  Parameters:
    self: the list which items will be mapped
    function: the function to apply to each element of the list

  Result:
    list: the list of "mapped" items

  A list of the "mapped" items is returned.

  If any item is mapped to an error object then the result is not a
  collection but the first (left-to-right-order) error object created.

  Topic: Lists

  See also: std_types::generic_list/std::reduce,
            std_types::generic_list/std::map_reduce

  Example:

    $values list(10 -5 20)
    map_or_error $result
      values
      to_string
    debug::dump 99 `result

  Error output:

    result:
      list
        "10"
        "-5"
        "20"

  Example:

    $divisors list(4 0 5)
    map_or_error $result
      divisors
      : (value) -> 100/value
    try $err:
      error_check result
    on err.is_an_error:
      println! "the result is an error object, not a list"

  Output:

    the result is an error object, not a list

((defined in basic/types/collections/lists/list.fky))
((generated by Hermes - an AI agent))
((2026-08-23 18:13:33))
