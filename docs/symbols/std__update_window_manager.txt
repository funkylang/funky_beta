# std::update_window_manager (a function)

  updates the window manager and terminal

  Parameters:
    io: the I/O object
    window_manager: the window manager object
    terminal: the terminal object
    fd: the file descriptor

  Result:
    io: the I/O object
    window_manager: the window manager object
    terminal: the terminal object

  The window manager and terminal are updated and the output is printed to
  the file descriptor.

  Topic: Window Manager

  See also: std::create_window_manager std::create_window std::set_menu
            std::set_status std::set_time std::handle_events
            std::handle_event std::handle_window_event

  Example:

    $io create_io()
    $terminal create_terminal(io)
    $wm create_window_manager(terminal)
    $window create_window(wm)
    $output update_window_manager(io wm terminal 1)
    dump! 2 `output

  Output:

    output:
      tuple
        io
        window_manager
        terminal

((defined in terminal/window_manager.fky))
((generated by Qwen3-Coder-30B-A3B-Instruct-Q4_0.gguf))
((2026-04-04 11:15:34))
