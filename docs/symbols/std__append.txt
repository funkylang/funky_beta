# std::append (a polymorphic function)

  appends two or more collections to form a single collection

  Parameters:
    collections*: two or more collections to concatenate

  Result:
    collection: a new collection containing all items from all arguments in order

  Returns a new collection with the items of all argument collections in order.
  The original collections are not modified.

  Topic: Collections

  See also: std::push, std::dup, std::concatenate

  Example:

    println! append("a " "b")

  Output:

    a b

((defined in runtime_templates/aaa.template))
((generated by Hermes - an AI agent))
((2026-07-28 13:14:32))
