# std_types::hash_table/: (a type function)

  retrieve, insert, or delete a key-value pair

  Parameters:
    key: the lookup key
    value?: new value to store (or undefined to delete)

  Result:
    the stored value on retrieval, or the modified table on insert/delete

  Calling a <std_types::hash_table> with a single key retrieves the
  associated value, or <undefined> if the key is not present.

  With two arguments, it inserts or updates the key-value pair and
  returns a cloned table with the new entry; the original table remains
  unchanged. If the key already exists, its value is replaced. The table
  length increases by one only when a new key is added.

  Passing <undefined> as the second argument deletes the key if it
  exists, returning a cloned table with the entry removed; the original
  table remains unchanged. Deleting a non-existent key returns the
  original table unchanged.

  Topic: Tables

  See also: std::empty_hash_table

  Example:

    $names empty_hash_table
    !names("Alice") 30
    !names("Bob") 25
    println! names("Alice")
    !names("Alice") 31
    println! names("Alice")
    !names("Bob") undefined
    println! length_of(names)

  Output:

    30
    31
    1

((defined in basic/types/collections/tables/hash_table.fky))
((generated by Hermes - an AI agent))
((2026-08-15 18:36:21))
