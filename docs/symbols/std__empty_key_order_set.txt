# std::empty_key_order_set (an object)

  returns an empty key-order set to use as a starting point

  Parameters:
    none

  Results:
    none

  This is an empty instance of the key_order_set type. Elements are iterated in
  sorted order based on their keys, unlike hash sets which have no fixed
  iteration order. Use it as the initial value and add elements with the call
  syntax:

    $my_set std::empty_key_order_set
    !my_set("item") true

  Topic: Collections

  See also: std_types::key_order_set, std::empty_insert_order_set

  Example:

    $months std::empty_key_order_set
    !months("march") true
    !months("january") true
    !months("february") true

    print! "Sorted months: " trim_left(debug::to_string(months 0 3))

  Output:

    Sorted months: key_order_set
      "february"
      "january"
      "march"

((defined in basic/types/collections/sets/key_order_set.fky))
((generated by Hermes - an AI agent))
((2026-08-30 13:25:20))
