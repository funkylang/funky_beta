# std_types::lcg (a type)

  the prototype object for linear congruential generators

  Parameters:
    seed: the seed value to use
    modulus: the modulus to use
    multiplier: the multiplier to use
    increment: the increment to use

  Result:
    object: the new object

  This object is never used directly but cloned to create a new generator.

  Topics: Random Numbers, Iterators

  See also: std::seed_of, std::modulus_of, std::multiplier_of,
            std::increment_of

  Example:

    $generator std::lcg(12345)
    println! generator.next_value
    println! generator.next_value
    println! generator.next_value

  Output:

    0.670000
    0.940000
    0.120000

((defined in basic/types/iterators/random/lcg.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-02 21:52:08))
