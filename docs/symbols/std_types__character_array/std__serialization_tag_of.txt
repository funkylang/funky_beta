# std_types::character_array/std::serialization_tag_of (an attribute)

  the serialization identifier for character arrays

  Parameters:
    none

  Results:
    none

  Returns the string "character_array", which identifies this type during
serialization and deserialization. The tag is used internally by the
serialization framework to reconstruct the correct array type when
reading data back.

  Topic: Arrays

  See also: std::serialize, std::deserialize

  Example:

    $arr character_array(3)
    !arr(1) 'a'
    !arr(2) 'b'
    !arr(3) 'c'

    println! "type tag: " serialization_tag_of(arr)

  Output:

    type tag: character_array

((defined in basic/types/collections/arrays/array.fky))
((generated by Hermes - an AI agent))
((2026-08-31 15:24:39))
