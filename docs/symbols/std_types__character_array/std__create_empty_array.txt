# std_types::character_array/std::create_empty_array (a method)

  creates an empty new array based on the given template array

  Parameters:
    template: an array

  Result:
    array: an empty array with the same dimensions as the template array

  Topic: Arrays

  See also: std_types::character_array/_type,
            std_types::character_array/length_of,
            std_types::character_array/dimension_count_of,
            std_types::character_array/equal,
            std::character_array,
            std::initialized_character_array,
            std_types::character_array/range

  Example:

    $chars character_array(3)
    !chars(1) 'A'
    !chars(2) 'B'
    !chars(3) 'C'

    $reset chars.create_empty_array
    print! "chars: " trim_left(debug::to_string(chars 0 3))
    print! "reset: " trim_left(debug::to_string(reset 0 3))

  Output:

    chars: <uchar32_array(1..3)>
    reset: <uchar32_array(1..3)>

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-30 20:17:35))
