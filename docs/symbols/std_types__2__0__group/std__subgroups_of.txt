# std_types::2::0::group/std::subgroups_of (an attribute)

  the child lines and nested groups under a group header

  A list of <std_types-2.0::line> objects and <std_types-2.0::group>
  objects that are nested under the header line of the group. Created
  by <std-2.0::split_into_groups>.

  Topic: Strings

  See also: std_types-2.0::group, std-2.0::split_into_groups

  Example:

    <using std-2.0>

    $text "
      a
        1
        2
      b
        3
        4
    $lines split_into_indented_lines(text)
    $groups split_into_groups(lines)
    $group groups(1)
    $sub subgroups_of(group)
    dump! `length_of(sub)

  Output:

    length_of(sub): 2

((defined in basic/types/collections/strings/string_2.fky))
((generated by Hermes - an AI agent))
((2026-08-06 20:54:10))
