# std::echo_new_lines (a polymorphic function)

  set or clear ECHONL in c_lflag

  Parameters:
    attributes: the terminal attributes
    do_set: if true then set ECHONL, otherwise clear ECHONL

  Result:
    new_attributes: the new terminal attributes

  Topic: Input-Output

  See also: std::use_canonical_mode, std::echo_characters,
    std::enable_xon_xoff_for_input, std::enable_xon_xoff_for_output,
    std::restart_output_on_any_character, std::ignore_cr_on_input,
    std::generate_signals, std::map_cr_to_lf_on_input, std::hangup_on_close,
    std::map_lf_to_crlf_on_output, std::minimum_characters_for_canoncial_read,
    std::timeout_for_reads, std::input_speed, std::output_speed

  Example:

    #
      retrieve the current terminal attributes
    #
    attributes = std::get_terminal_attributes(std::stdin)
    #
      check if echoing of new lines is enabled
    #
    if std::echo_new_lines(attributes) then
      #
        disable echoing of new lines
      #
      attributes = std::echo_new_lines(attributes, false)
      #
        update the terminal attributes
      #
      std::set_terminal_attributes(std::stdin, attributes)
    end if

((defined in runtime_templates/termio.template))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-03 16:54:01))
