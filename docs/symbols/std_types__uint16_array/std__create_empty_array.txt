# std_types::uint16_array/std::create_empty_array (a method)

  creates an empty new array based on the given template array

  Parameters:
    template: an array

  Result:
    array: an empty array with the same dimensions as the template array

  Topic: Arrays

  See also: std::uint16_array, std::initialized_uint16_array

  Example:

    $ports uint16_array(3)
    !ports(1) 80
    !ports(2) 443
    !ports(3) 8080

    $reset ports.create_empty_array
    print! "ports: " trim_left(debug::to_string(ports 0 3))
    print! "reset: " trim_left(debug::to_string(reset 0 3))

  Output:

    ports: <uint16_array(1..3)>
    reset: <uint16_array(1..3)>

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-30 20:17:34))
