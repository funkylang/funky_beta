# std_types::uint16_array/std::range (a method)

  creates a new unsigned 16-bit array with the elements in the given range

  Parameters:
    array: an unsigned 16-bit array
    ranges?: one or more value ranges, one per dimension

  Result:
    a new unsigned 16-bit array containing the selected elements

  The range argument is a value_range (start..end, inclusive). Supply one
range per dimension for multi-dimensional arrays. Returns a cloned array;
the original remains unchanged.

  Topic: Arrays

  See also: std::range, std_types::uint16_array/std::length_of

  Example:

    $print_array: (data)
      $row_count length_of(data 1)
      $column_count length_of(data 2)
      $separator_line append("+" dup("------------+" column_count))
      println! separator_line
      from_to 1 row_count
        : (row_no)
          print! "|"
          from_to 1 column_count
            : (col_no)
              $val data(row_no col_no)
              print! pad_left(val 10) "|"
              next!
          println!
          println! separator_line
          next!
        pass

    $values uint16_array(3 4)
    !values(1 1) 100
    !values(1 2) 200
    !values(1 3) 300
    !values(1 4) 400
    !values(2 1) 500
    !values(2 2) 600
    !values(2 3) 700
    !values(2 4) 800
    !values(3 1) 900
    !values(3 2) 1000
    !values(3 3) 1100
    !values(3 4) 1200

    $crop range(values 1..2 2..3)
    print_array! crop

  Output:

    +------------+------------+
    |       200|       300|
    +------------+------------+
    |       600|       700|
    +------------+------------+

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-31 10:48:40))
