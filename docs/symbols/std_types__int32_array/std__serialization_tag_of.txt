# std_types::int32_array/std::serialization_tag_of (an attribute)

  the serialization identifier for signed 32-bit arrays

  Parameters:
    none

  Results:
    none

  Returns the string "int32_array", which identifies this type during
serialization and deserialization. The tag is used internally by the
serialization framework to reconstruct the correct array type when
reading data back.

  Topic: Arrays

  See also: std::serialize, std::deserialize

  Example:

    $arr int32_array(3)
    !arr(1) 1024
    !arr(2) -512
    !arr(3) 2048

    println! "type tag: " serialization_tag_of(arr)

  Output:

    type tag: int32_array

((defined in basic/types/collections/arrays/array.fky))
((generated by Hermes - an AI agent))
((2026-08-31 09:16:37))
