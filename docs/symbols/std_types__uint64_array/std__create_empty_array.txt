# std_types::uint64_array/std::create_empty_array (a method)

  creates an empty new array based on the given template array

  Parameters:
    template: an array

  Result:
    array: an empty array with the same dimensions as the template array

  Topic: Arrays

  See also: std::uint64_array, std::initialized_uint64_array

  Example:

    $offsets uint64_array(3)
    !offsets(1) 0x2000
    !offsets(2) 0x5000
    !offsets(3) 0xA000

    $reset offsets.create_empty_array
    print! "offsets: " trim_left(debug::to_string(offsets 0 3))
    print! "reset: " trim_left(debug::to_string(reset 0 3))

  Output:

    offsets: <uint64_array(1..3)>
    reset: <uint64_array(1..3)>

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-30 20:17:34))
