# std_types::file_descriptor/std::write (a builtin I/O-method)

  a low level write function

  Parameters:
    stream: the stream to write to
    data: the data to be written

  Topic: Input-Output

  See also: std::read, std::write_line, std::write_string

  Example:

    # write a string to the standard output
    std::write(std::STDOUT_FILENO, "Hello, world!\n")

    # write a string to a file
    file = std::open("hello.txt", std::O_WRONLY|std::O_CREAT|std::O_TRUNC)
    std::write(file, "Hello, world!\n")
    std::close(file)

((defined in runtime_templates/posix.template))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-02 14:29:40))
