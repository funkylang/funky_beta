# std_types::file_descriptor/std::equal (a builtin method)

  compares two file descriptors for equality

  Parameters:
    left: a file descriptor
    right: a file descriptor

  Result:
    success: true if the two file descriptors are equal

  Topic: POSIX

  See also: std_types::file_descriptor, std_types::file_descriptor/std::hash

  Example:

    open! $fd "/dev/null" "w"
    if
      equal(fd, STDOUT_FILENO)
      println! "STDOUT_FILENO is a file descriptor to /dev/null"
      :
        println! "STDOUT_FILENO is not a file descriptor to /dev/null"

  Output:

    STDOUT_FILENO is a file descriptor to /dev/null

((defined in runtime_templates/posix.template))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-02 14:06:52))
