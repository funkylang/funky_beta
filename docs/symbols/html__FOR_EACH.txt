# html::FOR_EACH (a function)

  generates elements for each item in a collection

  Parameters:
    items: the collection of items
    body: a function that generates the element for a given item

  Result:
    a string containing the generated elements

  The function is used to generate elements for each item in a collection.

  If the body function takes two parameters, it is assumed that the items are key-value pairs.

  Topic: HTML

  Example:

    <require basic/stdlib>
    <require basic/export/html>

    <using std>
    <using html>

    println!
      FOR_EACH list("apple" "banana" "cherry")
	: (item)
	  P "Fruit: @(item)"

  Output:

    <p>Fruit: apple</p>
    <p>Fruit: banana</p>
    <p>Fruit: cherry</p>

((defined in basic/export/html.fky))
((generated by Qwen3-235B-A22B-Q4_K_M-00001-of-00003.gguf))
((rating 7))
((2025-05-10 18:58:11))
