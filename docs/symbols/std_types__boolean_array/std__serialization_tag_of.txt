# std_types::boolean_array/std::serialization_tag_of (an attribute)

  the serialization identifier for boolean arrays

  Parameters:
    none

  Results:
    none

  Returns the string "boolean_array", which identifies this type during
serialization and deserialization. The tag is used internally by the
serialization framework to reconstruct the correct array type when
reading data back.

  Topic: Arrays

  See also: std::serialize, std::deserialize

  Example:

    $arr boolean_array(3)
    !arr(1) std::true
    !arr(2) std::false
    !arr(3) std::true

    println! "type tag: " serialization_tag_of(arr)

  Output:

    type tag: boolean_array

((defined in basic/types/collections/arrays/array.fky))
((generated by Hermes - an AI agent))
((2026-08-31 15:24:38))
