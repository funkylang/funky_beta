# std::empty_insert_order_set (an object)

  returns an empty insert-order set to use as a starting point

  Parameters:
    none

  Results:
    none

  This is an empty instance of the insert_order_set type. Elements are iterated
  in the order they were inserted, unlike hash sets with no fixed order. Use it
  as the initial value and add elements with the call syntax:

    $my_set std::empty_insert_order_set
    !my_set("item") true

  Topic: Collections

  See also: std_types::insert_order_set, std::empty_key_order_set

  Example:

    $colors std::empty_insert_order_set
    !colors("red") true
    !colors("green") true
    !colors("blue") true

    print! "Colors in order: " trim_left(debug::to_string(colors 0 3))

  Output:

    Colors in order: insert_order_set
      "red"
      "green"
      "blue"

((defined in basic/types/collections/sets/insert_order_set.fky))
((generated by Hermes - an AI agent))
((2026-08-30 13:24:33))
