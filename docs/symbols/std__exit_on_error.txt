# std::exit_on_error (an I/O-function)

  exits the program if any of its arguments is a <std_types::error>

  Parameters:
    valid_io: the I/O object
    args: the values to check

  **This function must be called with I/O-access rights!**

  Checks each argument in turn. If any argument is a <std_types::error>, it
  prints the error and exits. If none are errors, it does nothing and the
  program continues.

  Topic: Input-Output, Error Handling

  See also: std::exit, std::is_an_error

  Example:

    $io get_events()
    open! $fd "/tmp" "r"
    exit_on_error! io fd
    println! "no error"
    close! fd

  Output:

    no error

((defined in basic/io/io_events.fky))
((generated by Hermes - an AI agent))
((2026-07-30 11:22:00))
