# std_types::stat/std::is_a_directory (a method)

  checks if the file is a directory

  Parameters:
    self: the file information

  Results:
    true if the file is a directory, false otherwise

  The function checks if the file is a directory by checking the mode of the
  file information.

  The file information can for a file can be retrieved by calling <std::stat>.

  Topic: Input-Output

  See also: std::is_a_directory, std::file_exists, std::stat

  Example:

    stat! $info "/usr/local/bin/funky"
    debug::dump `is_a_directory(info)

  Error output:

    is_a_directory(info): false

((defined in basic/io/io.fky))
((generated by Qwen_Qwen3-30B-A3B-Q4_K_M.gguf))
((revised))
((2025-05-07 11:26:00))
