# std_types::boolean_array (a type derived from std_types::generic_array)

  an array of boolean values

  Parameters:
    indices: the indices of the array elements
    value: the new value (only for updates)

  Result:
    value_or_array: the value at the specified position or the updated array

  Updates to arrays are delayed until the array is read.

  Array indices are one-based.

  Topic: Arrays

  See also: std_types::array

  Example:

    $a boolean_array(3 2) # a new array with 3 rows and 2 columns
    !a(2 1) true # use syntactic sugar to assign a value to an array element
    !a a(3 2 false) # without syntactic sugar
    dump! `a(2 1)
    dump! `a(3 2)

  Output:

    a(2 1): <true>
    a(3 2): <false>

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-21 19:16:05))