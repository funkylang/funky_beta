# std::echo_characters (a polymorphic function)

  set or clear ECHO in c_lflag

  Parameters:
    attributes: the terminal attributes
    do_set: if true then set ECHO, otherwise clear ECHO

  Result:
    new_attributes: the new terminal attributes

  Topic: Input-Output

  See also: std::use_canonical_mode, std::echo_new_lines,
    std::enable_xon_xoff_for_input, std::enable_xon_xoff_for_output,
    std::restart_output_on_any_character, std::ignore_cr_on_input,
    std::generate_signals, std::map_cr_to_lf_on_input, std::hangup_on_close,
    std::map_lf_to_crlf_on_output, std::minimum_characters_for_canoncial_read,
    std::timeout_for_reads, std::input_speed, std::output_speed

  Example:

    #
      set the echo flag
    #
    attributes = std::get_terminal_attributes(std::standard_input)
    if (attributes != undefined) {
      new_attributes = std::echo_characters(attributes, true)
      if (new_attributes != undefined) {
        std::set_terminal_attributes(std::standard_input, new_attributes)
      }
    }

((defined in runtime_templates/termio.template))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-03 16:53:36))
