# std::to_error_message_string (a function)

converts an error object to a string

  Parameter:
    err: the error object

  Result:
    the error message as a string

  The function converts an error object to a string. The error message is
  composed of the error message text, the error number and the error details.

  Topic: Errors

  See also: std::Error, std::error_message_text_of, std::errno_of,
            std::error_details_of

  Example:

    load! $buf "some-non-existing-file"
    on buf.is_an_error:
      println! to_error_message_string(buf)

  Output:

    OPEN FAILED - No such file or directory (some-non-existing-file)

((defined in basic/error.fky))
((generated by Hermes - an AI agent))
((2026-08-11 12:27:02))
