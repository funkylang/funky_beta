# std_types::hash_bag/std::serialization_tag_of (an attribute)

  returns the serialization tag of the hash bag

  Parameter:
    self: the hash bag

  Result:
    the serialization tag of the hash bag

  The serialization tag identifies the type in a serialized string.
  The deserializer uses it to route the string to the right
  reconstruction function.

  Topic: Bags

  See also: std_types::hash_bag

  Example:

    $basket empty_hash_bag
    !basket("apple") 2
    !basket("pear") 1
    !basket("banana") 3
    println! serialization_tag_of(basket)

  Output:

    hash_bag

((defined in basic/types/collections/bags/hash_bag.fky))
((generated by Hermes - an AI agent))
((2026-08-20 19:23:19))
