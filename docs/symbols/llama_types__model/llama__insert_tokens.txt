# llama_types::model/llama::insert_tokens (an I/O-method)

  inserts tokens into a sequence

  Parameters:
    model: the model
    sequence_id: the sequence id
    position: the position to insert the tokens at
    tokens: the tokens to insert

  Results:
    none

  **This function must be called with I/O-access rights!**

  The specified tokens are inserted into the server stored sequence at the
  specified position.

  Topic: AI

  See also: llama::add_tokens, llama::delete_tokens

((defined in ai/new_llama.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2025-04-20 10:04:13))
