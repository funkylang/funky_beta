# llama_types::model/llama::add_tokens (an I/O-method)

  adds tokens to a sequence

  Parameters:
    model: the model
    sequence_id: the sequence id
    tokens: the tokens to add

  Results:
    none

  **Attention**: This function must be called with I/O-access rights!

  The specified tokens are added to the server stored sequence (without
  evaluation).

  Topic: AI

  See also: llama::add_tokens, std_types::io/llama::add_tokens,
	    llama::add_token_and_evaluate,
	    llama_types::model/llama::add_token_and_evaluate,
	    llama::evaluate, llama_types::model/llama::evaluate

((defined in ai/new_llama.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((revised))
((2025-04-20 10:02:01))
