# std_types::undefined/std::generate_uuid (a method)

  generates a UUID

  Parameter:
    kind: <std::undefined>

  Result:
    a UUID string

  **This function must be called with I/O-access rights!**

  This function generates a UUID and returns it as a string.

  Topic: Authentication

  See also: std::generate_uuid

  Example:

    <require basic/stdlib>
    <require basic/uuid>

    std::generate_uuid! $uuid RANDOM_BASED_UUID
    println! "UUID: @(uuid)"

  Output:

    UUID: 1659c75a-198c-baee-cd3c-c4da824979cc

((defined in basic/uuid.fky))
((generated by Qwen_Qwen3-30B-A3B-Q4_K_M.gguf))
((revised))
((2025-05-15 13:39:59))
