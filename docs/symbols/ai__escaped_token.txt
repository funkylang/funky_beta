# ai::escaped_token (a function)

  escapes a token

  Parameters:
    model: the model
    token: the token

  Result:
    the escaped token

  Topic: Deprecated AI

  See also: ai::escaped_piece

  Example:

    $model ai::load_ai_model("sauerkrautlm-una-solar-instruct.Q5_K_M.gguf")
    $tokens ai::tokenize(model "Hello, World!")
    $token tokens(1)
    $escaped_token ai::escaped_token(model token)
    println! escaped_token

  Output:

    Hello, World!

((defined in ai/llama.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-03 16:04:54))
