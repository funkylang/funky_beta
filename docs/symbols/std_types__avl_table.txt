# std_types::avl_table (a type derived from std_types::binary_table)

  the prototype object for all AVL-based tables

  This object is never used directly but cloned to create a new table.

  *Currently, <std::map> does not work for <std_types::avl_table> due to a bug
  in its implementation.*

  Topics: Tables, Collections

  See also: std_types::binary_table, std_types::binary_table/std::map,
            std_types::binary_table/std::reduce,
            std_types::binary_table/std::map_reduce

  Example:

    <require basic/stdlib>
    <require basic/types/collections/tables/binary_table>
    <require basic/types/collections/tables/avl_table>

    $salaries empty_avl_table
    !salaries("Joe") 2500
    !salaries("Mary") 3000
    !salaries("Bob") 2000
    println! salaries("Mary")

  Output:

    3000

((defined in basic/types/collections/tables/avl_table.fky))
((generated by Hermes - an AI agent))
((2026-08-24 15:08:11))
