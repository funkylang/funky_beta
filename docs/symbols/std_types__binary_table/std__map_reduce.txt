# std_types::binary_table/std::map_reduce (a method)

  combines a map and a reduce operation into a single operation

  Does not create an intermediate table to hold the "mapped" values.

  Parameters:
    self: the table to work on
    map_function: the function to apply to each element of the table
    reduce_function: the function to apply to pairs of values
    unit: the value to return for empty tables

  Result:
    value: the result of the reduction

  Topic: Tables

  See also: std_types::binary_table/std::map,
            std_types::binary_table/std::reduce

  Example:

    <require basic/stdlib>
    <require basic/types/collections/tables/binary_table>

    $ages empty_binary_table
    !ages("Victor") 23
    !ages("Robert") 25
    !ages("Eva") 27
    println! map_reduce(ages: (name age) -> age)

  Output:

    75

((defined in basic/types/collections/tables/binary_table.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-02 23:02:52))
