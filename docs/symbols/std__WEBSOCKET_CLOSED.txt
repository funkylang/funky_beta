# std::WEBSOCKET_CLOSED (a unique item)

  the websocket closed event

  Topic: Network Clients and Servers

  See also: std::start_web_server, std::stop_web_server,
  std::handle_web_server_events

  Example:

    $io ...
    handle_web_server_events &io $events
    for_each events
      : (event)
        event $type $fd $argument
        case type
          WEBSOCKET_CLOSED:
            log &io -> "websocket closed - @(serialize(fd))"
            next
          ...

((defined in web/server.fky))
((generated by Codestral-22B-v0.1-Q5_K_M.gguf))
((2024-07-03 16:09:17))
