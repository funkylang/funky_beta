# std::to_wide (a polymorphic function)

  converts a character or a string to its wide character representation

  Parameters:
    chr # the character to convert
    str # the string to convert

  Result:
    chr # the wide character representation of the character
    str # the wide character representation of the string

  Topic: Characters

  See also: std_types::character/to_wide, std_types::string/to_wide

  Example:

    $str "Hello, World!"
    $wide_str to_wide(str)
    println! wide_str

  Output:

    Hello, World!

((defined in basic/types/character.fky))
((generated by gemma-2-27b-it.Q5_K.gguf))
((2024-07-03 21:26:10))
