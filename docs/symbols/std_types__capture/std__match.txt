# std_types::capture/std::match (a method)

  matches the expression and captures the matched text

  Parameters:
    self: the grammar object
    str: the string to match

  Result:
    the length of the match or <std::undefined>

  This method matches the expression and captures the matched text.

  Topics: Grammar Objects

  See also: std::capture, std_types::capture/std::match_back

  Example:

    $GLOBAL_DEFINITION
      '$', capture(FUNKY_IDENTIFIER), "::", capture(FUNKY_IDENTIFIER)
    $str "$std::print 123"
    match GLOBAL_DEFINITION str $len $captured
    println! len
    dump `captured

  Output:

    11
    captured: list <2>
      "std"
      "print"

((defined in basic/grammar.fky))
((generated by Qwen_Qwen3-30B-A3B-Q4_K_M.gguf))
((rating 5))
((2025-05-22 16:02:54))
