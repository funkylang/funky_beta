# std_types::set/std::match (a method)

  matches the start of a string with the set's characters

  Parameters:
    self: a set of characters
    str: the string whose start is matched

  Result:
    len: 1 if any character in the set matches the start of the string

  If the match is unsuccessful, then *len* is <undefined>.

  Use the <alt>-function to construct a set of characters.

  Topic: Strings

  See also: alt, match_back, search

  Example:

    $str "[abc]"
    $len match(alt('(' '[' '{') str)
    dump! `len

  Output:

    len: 1

  Example:

    $str "abc"
    $len match(alt('(' ']' '}') str)
    dump! `len

  Output:

    len: <undefined>

((defined in basic/types/collections/sets/set.fky))
((generated by Hermes - an AI agent))
((2026-08-21 15:03:38))
