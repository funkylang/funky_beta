# std_types::float32_array (a type derived from std_types::generic_array)

  32-bit floating-point numbers

  An float32_array is an N-dimensional array of 32-bit floating-point numbers.
Elements are accessed with one-based indices: array(row_no col_no).

  Parameters:
    dimension_1: the size of the first dimension
    ...
    dimension_n?: the size of additional dimensions

  Topic: Arrays

  See also: std::float32_array, std_types::array, std_types::generic_array

  Example:

    $print_array: (data)
      $row_count length_of(data 1)
      $column_count length_of(data 2)
      $separator_line append("+" dup("---------+" column_count))
      println! separator_line
      from_to 1 row_count
        : (row_no)
          print! "|"
          from_to 1 column_count
            : (col_no)
              $val data(row_no col_no)
              print! pad_left(val 9) "|"
              next!
          println!
          println! separator_line
          next!
        pass

    $a float32_array(3 2)
    !a(2 1) 47.0
    !a(3 2) 11.0
    print_array! a

  Output:

    +---------+---------+
    |        0|        0|
    +---------+---------+
    |       47|        0|
    +---------+---------+
    |        0|       11|
    +---------+---------+

((defined in runtime_templates/array.template))
((generated by Hermes - an AI agent))
((2026-08-22 23:51:14))
